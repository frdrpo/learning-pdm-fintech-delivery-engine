# Build + dependency output
dist/
node_modules/
.node_modules/

# PDM run artifacts (written to ephemeral GitHub runners or local dry-runs)
.github/pdm/deployments/
.github/pdm/reports/
.github/pdm/releases/

# Local opencode runtime config (model pins, per-machine); the canonical
# scrubbed fleet lives in agents/ (ADR 0015). Copy agents/opencode.example.json
# to opencode.json if you want a tracked-free local runtime.
opencode.json

# Local opencode runtime installs/deps — never repo content (ADR 0015);
# the canonical fleet is agents/, installed locally via `make fleet-sync`.
.opencode

# Throwaway wiki clone used for wiki-only edits (ADR 0013)
.wiki-tmp/