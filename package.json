{
  "name": "learning-pdm-fintech-delivery-engine",
  "version": "0.1.0",
  "description": "Reference Product Delivery Management engine — minimal Node service that exercises the PDM delivery gates",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "lint": "eslint src scripts",
    "test": "node --test \"src/*.test.js\"",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "eslint": "^9.22.0"
  }
}